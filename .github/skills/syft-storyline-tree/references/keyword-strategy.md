# Keyword Strategy

This file is retained for compatibility, but active backfill work should use `syft-storyline-backfill`.

If you are still inside `syft-storyline-tree` and need to think about search terms, use the same principles:

- do not search the branch title blindly
- derive angles from actors, mechanism, consequence, geography, and jargon
- keep only results that deepen the branch timeline
